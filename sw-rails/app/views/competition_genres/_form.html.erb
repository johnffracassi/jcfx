<%= form_for(@competition_genre) do |f| %>
  <% if @competition_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competition_genre.errors.count, "error") %> prohibited this competition_genre from being saved:</h2>

      <ul>
      <% @competition_genre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
