<%= form_for(@extra_type) do |f| %>
  <% if @extra_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extra_type.errors.count, "error") %> prohibited this extra_type from being saved:</h2>

      <ul>
      <% @extra_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
