<%= form_for(@player_alias) do |f| %>
  <% if @player_alias.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_alias.errors.count, "error") %> prohibited this player_alias from being saved:</h2>

      <ul>
      <% @player_alias.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
