<%= form_for(@dismissal_type) do |f| %>
  <% if @dismissal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dismissal_type.errors.count, "error") %> prohibited this dismissal_type from being saved:</h2>

      <ul>
      <% @dismissal_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
