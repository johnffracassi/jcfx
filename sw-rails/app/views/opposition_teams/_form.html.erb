<%= form_for(@opposition_team) do |f| %>
  <% if @opposition_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opposition_team.errors.count, "error") %> prohibited this opposition_team from being saved:</h2>

      <ul>
      <% @opposition_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
